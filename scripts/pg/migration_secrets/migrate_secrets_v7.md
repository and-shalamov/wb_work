Назначение скрипта
Скрипт предназначен для миграции Kubernetes секретов между кластерами с возможностью фильтрации, переименования и преобразования форматов.

Основные возможности
🔄 Миграция секретов между кластерами Kubernetes

🎯 Фильтрация по типу секретов (PostgreSQL, Redis, приложения)

📁 Автоматическое создание структуры папок с timestamp

🔐 Преобразование имен секретов (например: .acid- → .psql-)

📊 Анализ секретов по веткам и проектам

✅ Сравнение данных до и после миграции

🚀 Автоматическое создание скрипта применения

Синтаксис
bash
./migrate_secrets_v7.sh [--debug] <old_context> <new_context> <namespace> [project] [branch] [filter]
Параметры
Параметр	Обязательный	Описание	Пример
old_context	✅	Исходный кластер Kubernetes	old-cluster
new_context	✅	Целевой кластер Kubernetes	new-cluster
namespace	✅	Kubernetes namespace	default, production
project	❌	Название проекта	pickup, delivery
branch	❌	Ветка/окружение	main, stage, dev
filter	❌	Фильтр секретов (по умолчанию: postgres)	postgres, redis, app
--debug	❌	Подробный вывод	--debug
Примеры использования
1. Миграция всех PostgreSQL секретов в namespace
bash
./migrate_secrets_v7.sh old-cluster new-cluster default
2. Миграция секретов конкретного проекта
bash
./migrate_secrets_v7.sh old-cluster new-cluster default pickup
3. Миграция секретов конкретной ветки проекта
bash
./migrate_secrets_v7.sh old-cluster new-cluster default pickup main
4. Миграция Redis секретов
bash
./migrate_secrets_v7.sh old-cluster new-cluster default "" "" redis
5. С debug выводом
bash
./migrate_secrets_v7.sh --debug old-cluster new-cluster default pickup main
Поддерживаемые фильтры
postgres (по умолчанию) - секреты PostgreSQL с трансформацией имен

redis - секреты Redis

app - секреты приложений

любая строка - кастомный фильтр для grep

Трансформация имен (для postgres фильтра)
Скрипт автоматически преобразует имена секретов:

acid-<project>-<branch>-dbs → psql-<project>-<branch>

Убирает суффикс -dbs из имен

Пример:

Старое имя: acid-pickup-main-dbs.postgres.acid.zalan.do

Новое имя: psql-pickup-main.postgres.acid.zalan.do

Структура создаваемых файлов
Скрипт создает папку по шаблону:
<namespace>-<branch|all>-<filter>-<дата>-<время_чч_мм>

text
default-all-postgres-20231201-14_30/
├── old_secrets/          # Исходные секреты в JSON
│   ├── secret1.json
│   └── secret2.json
└── new_secrets/          # Новые секреты в YAML + скрипт
    ├── secret1.yaml
    ├── secret2.yaml
    └── apply_secrets.sh  # Скрипт применения
Процесс работы
1. Подготовка
Проверка аргументов

Создание структуры папок

Поиск секретов по фильтру

2. Миграция
Экспорт секретов из старого кластера (JSON)

Трансформация имен (для postgres)

Создание новых секретов (YAML)

3. Валидация
Сравнение данных первого секрета

Анализ по веткам

Создание скрипта применения

4. Применение
Автоматическое или ручное применение секретов

Проверка результатов

Скрипт применения
Автоматически создается файл apply_secrets.sh с содержимым:

bash
#!/bin/bash
# Применяет все YAML файлы в текущей папке к целевому кластеру
kubectl --context=new-cluster apply -f secret1.yaml
kubectl --context=new-cluster apply -f secret2.yaml
# ...
Цветовая схема вывода
🔴 Красный - ошибки

🟢 Зеленый - успешные операции

🟡 Желтый - предупреждения и информация

🔵 Синий - заголовки и шаги

🟣 Фиолетовый - структура файлов

🟠 Оранжевый - debug информация

Особенности работы
Автоматическое определение веток
Скрипт анализирует имена секретов для определения веток:

acid-pickup-main-dbs → ветка main

acid-delivery-stage-dbs → ветка stage

Сравнение данных
Для первого секрета выполняется сравнение:

Username

Password

Результат сравнения выводится на экран

Обработка ошибок
Продолжение работы при ошибках отдельных секретов

Подсчет успешных и неудачных миграций

Детальные сообщения об ошибках в debug режиме

Практические сценарии
Миграция всех секретов проекта между stage и prod
bash
# Stage -> Production
./migrate_secrets_v7.sh stage-cluster prod-cluster default pickup
Миграция только dev окружения
bash
./migrate_secrets_v7.sh old-cluster new-cluster default pickup dev
Проверка перед миграцией (debug режим)
bash
./migrate_secrets_v7.sh --debug old-cluster new-cluster default pickup main
Требования
Доступ к обоим Kubernetes кластерам

Настроенные kubeconfig контексты

Утилиты: kubectl, jq, yq, base64

Права на чтение секретов в исходном кластере

Права на создание секретов в целевом кластере

Рекомендации
Перед миграцией убедитесь в правильности контекстов

Используйте debug режим для первой миграции

Проверяйте сравнение данных перед применением

Сохраняйте созданные папки для аудита

Тестируйте на stage перед prod миграцией

Устранение неполадок
Ошибка "Не найдено секретов"
Проверьте namespace

Проверьте фильтр

Убедитесь в доступе к кластеру

Ошибка применения
Проверьте права в целевом кластере

Убедитесь что секрет с таким именем не существует

Проверьте формат YAML файлов

Debug информация
Используйте --debug для подробного вывода:

bash
./migrate_secrets_v7.sh --debug old-cluster new-cluster default pickup main
Этот скрипт значительно упрощает процесс миграции секретов между кластерами, обеспечивая безопасность и отслеживаемость изменений.