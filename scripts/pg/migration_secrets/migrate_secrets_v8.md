📋 Назначение
Скрипт migrate_secrets_v7.sh предназначен для миграции секретов (Secrets) между кластерами Kubernetes. Он автоматически экспортирует секреты из исходного кластера, преобразует их в необходимый формат и создает готовые к применению файлы для целевого кластера.

🚀 Возможности
Поддержка фильтров: Миграция только определенных типов секретов (postgres, redis, app и др.)

Селективная миграция: Возможность миграции по проектам и веткам

Автоматическое переименование: Трансформация имен секретов (например, .acid- → .psql- для postgres)

Сохранение типа секрета: Перенос оригинального типа секрета (Opaque, TLS и т.д.)

Сравнение данных: Проверка корректности миграции через сравнение credentials

Подробное логирование: Поддержка debug-режима для отладки

Автоматическое создание скриптов: Генерация готовых скриптов для применения секретов

📋 Требования
Необходимые инструменты
kubectl: Утилита командной строки для Kubernetes

jq: Утилита для обработки JSON

yq: Утилита для обработки YAML (версия 4+)

bash: Версия 4.0 или выше

Проверка зависимостей
bash
# Проверка наличия необходимых утилит
which kubectl jq yq

# Проверка версии bash
bash --version
📖 Использование
Базовый синтаксис
bash
./migrate_secrets_v7.sh [--debug] <old_context> <new_context> <namespace> [project] [branch] [filter]
Параметры
Параметр	Обязательный	Описание	По умолчанию
old_context	Да	Контекст kubectl исходного кластера	-
new_context	Да	Контекст kubectl целевого кластера	-
namespace	Да	Kubernetes Namespace для миграции	-
project	Нет	Имя проекта для фильтрации (например, "pickup")	все проекты
branch	Нет	Имя ветки для фильтрации (например, "main")	все ветки
filter	Нет	Фильтр по имени секрета (регулярное выражение)	"postgres"
--debug	Нет	Включение подробного вывода	выключен
Примеры использования
1. Миграция всех postgres-секретов в namespace "default"
bash
./migrate_secrets_v7.sh old-cluster new-cluster default
2. Миграция всех секретов проекта "pickup" в namespace "production"
bash
./migrate_secrets_v7.sh old-cluster new-cluster production pickup
3. Миграция конкретной ветки проекта с debug-выводом
bash
./migrate_secrets_v7.sh --debug old-cluster new-cluster default pickup main
4. Миграция redis-секретов
bash
./migrate_secrets_v7.sh old-cluster new-cluster default '' '' redis
5. Миграция всех секретов с фильтром по приложению
bash
./migrate_secrets_v7.sh old-cluster new-cluster default '' '' app
🔧 Подробное описание работы
1. Подготовка
Скрипт создает структуру папок с именем по шаблону:

text
<namespace>-<branch|all>-<filter>-<timestamp>/
├── old_secrets/     # Экспортированные секреты в JSON
└── new_secrets/     # Преобразованные секреты в YAML + скрипт применения
Пример имени папки: default-all-postgres-20241210-14_30

2. Поиск секретов
В зависимости от параметров, скрипт ищет секреты:

Без указания проекта: Все секреты в namespace, соответствующие фильтру

С указанием проекта: Секреты, содержащие имя проекта (и ветку при указании)

3. Обработка имен секретов
Для фильтра "postgres" выполняется автоматическое переименование:

.acid- → .psql-

Удаление -dbs. из имени

Пример: acid-pickup-main-dbs.postgresql.acid.zalan.do → psql-pickup-main.postgresql.acid.zalan.do

4. Экспорт и преобразование
Для каждого секрета:

Экспорт в JSON из старого кластера

Извлечение данных и типа секрета

Создание YAML-манифеста для нового кластера

Сохранение оригинального типа секрета

5. Создание скрипта применения
В папке new_secrets создается скрипт apply_secrets.sh, который:

Применяет все YAML-файлы в целевой кластер

Выводит отчет об успешности операции

Проверяет наличие примененных секретов

6. Проверка миграции
Для первого секрета выполняется сравнение:

Имя пользователя (username)

Пароль (password)

Проверяется совпадение данных

📊 Типы секретов
Скрипт поддерживает следующие типы секретов (сохраняются при миграции):

Тип	Описание
Opaque	Произвольные данные (по умолчанию)
kubernetes.io/tls	TLS сертификаты
kubernetes.io/dockerconfigjson	Docker Registry credentials
kubernetes.io/basic-auth	Базовая аутентификация
kubernetes.io/ssh-auth	SSH ключи
kubernetes.io/service-account-token	Service Account токены
⚙️ Настройка фильтров
Предопределенные фильтры
Фильтр	Описание	Пример использования
postgres	PostgreSQL секреты (по умолчанию)	Для миграции БД
redis	Redis секреты	Для миграции кэша
app	Секреты приложений	Для миграции конфигураций
tls	TLS сертификаты	Для миграции SSL
Кастомные фильтры
Можно использовать любое регулярное выражение:

bash
# Миграция всех секретов, содержащих "prod"
./migrate_secrets_v7.sh old-cluster new-cluster default '' '' prod

# Миграция всех секретов с префиксом "api-"
./migrate_secrets_v7.sh old-cluster new-cluster default '' '' ^api-

# Миграция секретов для мониторинга
./migrate_secrets_v7.sh old-cluster new-cluster default '' '' '(prometheus|grafana)'
🎯 Особенности работы с ветками
Паттерны именования
Скрипт распознает следующие паттерны имен секретов:

acid-<project>-<branch>-dbs (например: acid-pickup-main-dbs.postgresql.acid.zalan.do)

acid-<project>-<branch> (например: acid-pickup-main.credentials)

Анализ по веткам
В debug-режиме скрипт предоставляет статистику:

text
=== Анализ json секретов по веткам (фильтр: postgres) ===
Всего json файлов: 15

Распределение по веткам:
  main: 8 файлов (53%)
  dev: 5 файлов (33%)
  staging: 2 файлов (13%)
🔍 Debug режим
Включение debug-режима
bash
./migrate_secrets_v7.sh --debug old-cluster new-cluster default
Что выводится в debug-режиме
Детальная информация о каждом секрете:

Ключи данных

Тип секрета

Проект и ветка

Проверка созданных файлов:

Список файлов в папках

Размеры файлов

Расширенное логирование:

Все шаги выполнения

Промежуточные результаты

⚠️ Предупреждения и ограничения
Ограничения
Доступ к кластерам: Необходимы права на чтение в исходном кластере и запись в целевом

Объем данных: Секреты с большим объемом данных могут требовать дополнительной обработки

Бинарные данные: Секреты с бинарными данными (например, сертификаты) мигрируются корректно

Рекомендации
Тестирование: Всегда тестируйте миграцию на тестовых кластерах

Бэкап: Перед миграцией убедитесь в наличии бэкапов

Время миграции: Для больших объемов планируйте миграцию в период низкой нагрузки

🔄 Обработка ошибок
Коды возврата
Код	Описание
0	Успешное выполнение
1	Ошибка в аргументах или конфигурации
2	Ошибка при экспорте секретов
3	Ошибка при создании файлов
Типичные ошибки и решения
"Не найдено секретов с фильтром"

Проверьте правильность фильтра

Убедитесь, что секреты существуют в namespace

"Ошибка декодирования" при сравнении

Проверьте формат данных в секретах

Убедитесь в наличии утилит base64

"Ошибка при применении"

Проверьте права доступа в целевом кластере

Убедитесь, что namespace существует

📁 Структура выходных данных
После выполнения скрипта создается следующая структура:

text
migration-scripts/
├── migrate_secrets_v7.sh
└── default-all-postgres-20241210-14_30/
    ├── old_secrets/
    │   ├── acid-pickup-main-dbs.postgresql.acid.zalan.do.json
    │   └── acid-pickup-dev-dbs.postgresql.acid.zalan.do.json
    └── new_secrets/
        ├── psql-pickup-main.postgresql.acid.zalan.do.yaml
        ├── psql-pickup-dev.postgresql.acid.zalan.do.yaml
        └── apply_secrets.sh
Файлы
Файл	Формат	Назначение
*.json	JSON	Экспортированные секреты из старого кластера
*.yaml	YAML	Готовые манифесты для нового кластера
apply_secrets.sh	Bash	Скрипт для применения секретов
🛠️ Расширение функциональности
Добавление новых фильтров
Для добавления обработки новых типов секретов, модифицируйте функцию generate_new_secret_name:

bash
# Пример добавления обработки для MongoDB
if [ "$filter" = "mongodb" ]; then
    # Трансформация для MongoDB
    local new_name="${old_name/.mongo-/.mongodb-}"
    echo "$new_name"
fi
Кастомная обработка данных
Для дополнительной обработки данных можно модифицировать функцию migrate_single_secret:

bash
# Пример: добавление аннотаций
cat >> "$new_secret_file" << EOF
metadata:
  annotations:
    migrated-by: "migrate_secrets_v7.sh"
    migrated-at: "$(date -Iseconds)"
EOF
📝 История изменений
Версия 7.0
Добавлено: Сохранение типа секрета при миграции

Улучшено: Более детальная обработка ошибок

Оптимизировано: Улучшена производительность для больших объемов

Добавлено: Расширенная документация

Версия 6.0
Добавлено: Поддержка кастомных фильтров

Улучшено: Анализ по веткам

Добавлено: Сравнение credentials

🤝 Вклад в разработку
Форкните репозиторий

Создайте ветку для новой функции (git checkout -b feature/new-feature)

Зафиксируйте изменения (git commit -am 'Add new feature')

Запушьте ветку (git push origin feature/new-feature)

Создайте Pull Request

📄 Лицензия
Этот скрипт распространяется под лицензией MIT.

👥 Авторы
Команда DevOps

Поддержка: devops-team@example.com

Примечание: Перед использованием в production среде всегда тестируйте скрипт в staging-окружении.