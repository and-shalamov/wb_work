Назначение скрипта
Скрипт предназначен для комплексной проверки состояния Patroni кластера и репликации PostgreSQL в Kubernetes окружении. Он предоставляет детальную информацию о состоянии репликации, лагах, размерах баз данных и логах.

Синтаксис
bash
./check_replication_v5.sh <pod_name> <port> <namespace> <patroni_version> [password]
Параметры
Параметр	Обязательный	Описание	Пример
pod_name	✅	Имя пода Patroni	acid-pickup-main-0
port		✅	Порт PostgreSQL	5432
namespace	✅	Kubernetes namespace	default
patroni_version	✅	Версия Patroni (v2 или v4)	v4
password	❌	Пароль PostgreSQL (если не указан, будет получен из секрета)	mysecretpassword
Примеры использования
Базовая проверка (пароль будет получен автоматически)
bash
./check_replication_v5.sh acid-pickup-main-0 5432 default v4
С указанием пароля
bash
./check_replication_v5.sh acid-pickup-main-0 5432 default v4 mypassword
Для Patroni v2
bash
./check_replication_v5.sh acid-delivery-stage-0 5432 staging v2
Что проверяет скрипт
1. Статус Patroni через API
Проверяет роль ноды (master, replica, standby_leader)

Состояние репликации

Использует разные API endpoints для v2 и v4

2. Статус через patronictl
Обзор всего кластера Patroni

Роли всех нод

Состояние репликации

3. Статус репликации PostgreSQL
Подключенные реплики

Состояние синхронизации

IP-адреса реплик

4. Лаг репликации
Размер лага в байтах

Человеко-читаемый размер лага

Имена приложений реплик

5. Слоты репликации
Активность слотов

Размер лага для каждого слота

Типы слотов

6. Размер базы данных
Размер каждой базы данных

Сортировка по убыванию размера

7. Размер data directory
Фактический размер на диске

Использование du для точного измерения

8. Активность WAL
Последние LSN операции

Лаг воспроизведения WAL

Статус паузы репликации

9. Статус получения WAL (для standby)
Время последних сообщений

Текущий LSN

Размер лага

10. Табличные пространства
Расположение tablespace

Размер каждого tablespace

11. Активные подключения
Количество подключений по базам

Состояние подключений

Пользователи

12. Логи пода
ERROR логи (красные) - последние 5 сообщений

WARN логи (оранжевые) - последние 5 сообщений

INFO логи (синие) - последние 5 сообщений

DEBUG логи (голубые) - последние 5 сообщений

FATAL логи (фиолетовые) - последние 5 сообщений

Общий обзор (последние 10 строк)

Автоматическое получение пароля
Если пароль не указан явно, скрипт автоматически:

Ищет секреты в указанном namespace

Фильтрует по имени пода и ключевым словам (acid, psql, postgresql, postgres)

Предлагает выбрать секрет если найдено несколько

Пробует разные ключи в секрете (password, postgres-password, pgpassword, etc.)

Извлекает и использует пароль

Критерии поиска секретов:
Содержит имя пода (без числового суффикса)

ИЛИ содержит ключевые слова: acid, psql, postgresql, postgres

Цветовая схема вывода
🔴 Красный - ошибки и проблемы

🟢 Зеленый - успешные операции

🟡 Желтый - предупреждения и информация

🔵 Синий - заголовки и шаги

🟣 Фиолетовый - разделы и FATAL логи

🟠 Оранжевый - WARN логи

🌊 Голубой - DEBUG логи и детали

Особенности для разных версий Patroni
Patroni v4
API endpoint: http://127.0.0.1:8008/

Поля: role, replication_state

Ожидаемый результат: {"role":"standby_leader","replication_state":"streaming"}

Patroni v2
API endpoint: http://127.0.0.1:8008/

Поля: role, state

Ожидаемый результат: {"role":"standby_leader","state":"running"}

Дополнительные команды для мониторинга
Скрипт также предоставляет готовые команды для ручной проверки:

Мониторинг Patroni
bash
# Панель мониторинга Patroni
kubectl exec -it -n <namespace> <pod_name> -- patronictl list

# Мониторинг в реальном времени
watch -n1 'kubectl exec -it -n <namespace> <pod_name> -- patronictl list'
Анализ таблиц
bash
# Топ самых больших таблиц
kubectl exec -it -n <namespace> <pod_name> -- bash -c "PGPASSWORD=\"<password>\" psql -U postgres -c \"SELECT schemaname, tablename, pg_size_pretty(pg_total_relation_size(schemaname||'.'||tablename)) as size FROM pg_tables WHERE schemaname NOT IN ('pg_catalog', 'information_schema') ORDER BY pg_total_relation_size(schemaname||'.'||tablename) DESC LIMIT 10;\""
Просмотр логов
bash
# Последние 50 строк логов
kubectl logs -n <namespace> <pod_name> --tail=50

# Логи в реальном времени
kubectl logs -n <namespace> <pod_name> -f
Интерпретация результатов
Критически важные показатели:
Роль ноды - должна соответствовать ожидаемой (master/replica)

Состояние репликации - должно быть streaming

Лаг репликации - должен быть минимальным (желательно < 1MB)

Активность WAL - воспроизведение не должно быть на паузе

ERROR логи - не должно быть критических ошибок

Нормальные значения:
Replication lag: < 100 MB (в идеале < 1 MB)

Replay lag: < 10 секунд

WAL replay: не на паузе

Slot lag: минимальный

Connections: разумное количество активных подключений

Устранение неполадок
Если скрипт не находит пароль:
Укажите пароль явно в аргументах

Проверьте что секрет существует в namespace

Убедитесь что у вас есть права на чтение секретов

Если API Patroni недоступно:
Проверьте что под запущен

Убедитесь что Patroni работает на порту 8008

Проверьте логи Patroni

Если репликация не работает:
Проверьте логи на мастере и репликах

Убедитесь в сетевой доступности

Проверьте настройки репликации в Patroni

Безопасность
Пароль не выводится в открытом виде в логах

Подключение к PostgreSQL использует SSL (если настроено)

Секреты читаются только при необходимости

Этот скрипт предоставляет полную картину состояния Patroni кластера и помогает быстро диагностировать проблемы с репликацией PostgreSQL.