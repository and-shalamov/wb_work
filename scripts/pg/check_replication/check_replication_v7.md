Описание
Скрипт check_replication_v7.sh предназначен для комплексной проверки статуса Patroni и репликации PostgreSQL в Kubernetes-окружении. Скрипт автоматически определяет необходимые секреты и предоставляет детальную информацию о состоянии кластера.

Требования
Доступ к Kubernetes кластеру

Установленный kubectl

Утилиты: jq, base64, psql

Права на выполнение команд в подах

Использование
Базовый синтаксис
bash
./check_replication_v7.sh <pod_name> <port> <namespace> <patroni_version> [password]
Параметры
pod_name - имя Pod'а с PostgreSQL (например: psql-medicine-chest-stage1-0)

port - порт PostgreSQL (обычно 5432)

namespace - Kubernetes namespace

patroni_version - версия Patroni (v2 или v4)

password - опционально, пароль PostgreSQL (если не указан, будет получен автоматически)

Примеры использования
bash
# Автоматический поиск пароля
./check_replication_v7.sh psql-medicine-chest-stage1-0 5432 my-namespace v4

# С указанием пароля
./check_replication_v7.sh psql-medicine-chest-stage1-0 5432 my-namespace v4 mypassword

# Для Patroni v2
./check_replication_v7.sh psql-medicine-chest-main-0 5432 production v2
Особенности версии v7
Умный поиск секретов
Скрипт автоматически:

Извлекает имя ветки из имени пода (например, из psql-medicine-chest-stage1-0 извлекается stage1)

Фильтрует секреты по критериям:

Должны содержать имя ветки (stage1, main, и т.д.)

И содержать одно из ключевых слов: acid, postgres, psql

Пример фильтрации
Для пода psql-medicine-chest-stage1-0 будут предложены только:

text
- iamtoucanuser.psql-medicine-chest-stage1.credentials.postgresql.acid.zalan.do
- postgres.psql-medicine-chest-stage1.credentials.postgresql.acid.zalan.do
- standby.psql-medicine-chest-stage1.credentials.postgresql.acid.zalan.do
Автоматическое определение режима базы
Скрипт определяет, является ли база:

Мастером - показывает текущую WAL позицию

Репликой - показывает информацию о получении и применении WAL

Что проверяет скрипт
1. Статус Patroni
API статус Patroni

Статус через patronictl

2. Репликация PostgreSQL
Статус репликационных потоков

Лаг репликации в байтах и читаемом формате

Слоты репликации

3. Мониторинг производительности
Размеры баз данных

Размер data directory

Активные подключения

Табличные пространства

4. WAL активность
Для реплик: получение и применение WAL

Для мастера: текущая WAL позиция

5. Логи
Цветной вывод логов по уровням (ERROR, WARN, INFO, DEBUG, FATAL)

Последние сообщения каждого уровня

Цветовая схема вывода
🔴 RED - ошибки и критические сообщения

🟢 GREEN - успешные операции

🟡 YELLOW - предупреждения и выборы

🔵 BLUE - информационные сообщения

🟣 PURPLE - заголовки разделов

🟠 ORANGE - WARN логи

Дополнительные команды
В конце скрипт предоставляет полезные команды для дальнейшего мониторинга:

Панель мониторинга Patroni

Топ самых больших таблиц

Мониторинг в реальном времени

Просмотр логов

Особенности работы
Безопасность
Пароль не отображается в выводе

При ошибках получения пароля предлагается указать его явно

Обработка ошибок
Автоматическое завершение при критических ошибках

Цветовая индикация проблем

Подробные сообщения об ошибках

Совместимость
Поддержка Patroni v2 и v4

Автоматическая адаптация к роли базы (мастер/реплика)

Поддержка различных схем именования секретов

Советы по использованию
Для первого запуска рекомендуется использовать автоматический поиск пароля

При проблемах с подключением укажите пароль явно в параметрах

Для регулярного мониторинга используйте дополнительные команды из раздела "Дополнительные команды"

При анализе проблем обращайте внимание на цветовую маркировку логов

Скрипт значительно упрощает мониторинг и диагностику кластеров PostgreSQL с Patroni в Kubernetes-окружении.